; -----------------------------------------------------------------------------
section .text
bits 64
; -------------------------------------

global klaunch
extern kmain

klaunch:
    mov     rax, 0x2F592F412F4B2F4F
    mov     [0xB8000], rax
;    call    kmain
.halt:
    hlt
    jmp     .halt
.end:
; -----------------------------------------------------------------------------
